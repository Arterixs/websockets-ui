import { AttackClient } from '../types/interface/attack.js';
import { Socket } from '../types/types/common.js';

export const attackShips = (object: AttackClient, _socket: Socket) => {
  console.log(object);
};
